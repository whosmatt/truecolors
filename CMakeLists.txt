# ESP32-S3-WROOM-1 (N8R8).
cmake_minimum_required(VERSION 3.16)

# Components live under components/ (default) and the app entry under main/.
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(truecolors)
