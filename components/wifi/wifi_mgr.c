// wifi_mgr.c STUB
#include "wifi_mgr.h"
#include "esp_log.h"

static const char *TAG = "wifi";

esp_err_t wifi_start(void)
{
    ESP_LOGI(TAG, "wifi start (stub)");
    return ESP_OK;
}

esp_err_t wifi_enter_ap(void)
{
    ESP_LOGI(TAG, "wifi enter AP (stub)");
    return ESP_OK;
}
